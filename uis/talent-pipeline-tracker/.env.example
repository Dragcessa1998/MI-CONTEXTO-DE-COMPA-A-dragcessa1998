# URL base de la API del tracker del curso (Hito 3).
# Copia este archivo a .env.local (NO se commitea) antes de arrancar la app.
NEXT_PUBLIC_API_URL=https://playground.4geeks.com/tracker/api/v1

# AUTH-01 FastAPI service used for users, profiles, and JWT validation.
NEXT_PUBLIC_AUTH_API_URL=http://localhost:8000
